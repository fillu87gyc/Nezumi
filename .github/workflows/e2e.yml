name: E2E

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

# 同一ブランチの古いランは打ち切る（ハングしたランが 60 分枠を燃やし続けるのを防ぐ）
concurrency:
  group: e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@a7487c7e89a18df4991f7f222e4898a00d66ddda # v4.1.0
        with:
          version: 10

      # Node 24 だと `playwright install` がブラウザ展開後にプロセスが終了せず
      # 無限ハングする（展開自体は成功している）。ローカル検証済みの Node 22 に揃える。
      # 実行環境は workerd なので CI の Node はツールチェーンにのみ影響する。
      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck (Workers)
        run: npx tsc --noEmit

      - name: Unit tests
        run: pnpm test

      - name: Build client (PWA)
        run: pnpm build

      - name: Get Playwright version
        id: pw-version
        run: echo "version=$(node -e "console.log(require('./node_modules/@playwright/test/package.json').version)")" >> $GITHUB_OUTPUT

      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.pw-version.outputs.version }}

      - name: Install Playwright system dependencies
        run: npx playwright install-deps chromium

      # ダウンロードが稀に無言でハングするため、1 回 3 分で打ち切って最大 3 回試す。
      # DEBUG=pw:install でどのアーティファクト（chromium / headless shell / ffmpeg）で
      # 止まったかをログに残す。
      - name: Install Playwright Chromium
        if: steps.pw-cache.outputs.cache-hit != 'true'
        timeout-minutes: 12
        env:
          DEBUG: pw:install
        run: |
          for attempt in 1 2 3; do
            if timeout 180 npx playwright install chromium; then
              exit 0
            fi
            echo "::warning::playwright install chromium hung or failed (attempt ${attempt}/3), retrying..."
          done
          exit 1

      - name: Run E2E tests
        run: pnpm test:e2e

      # 全機能のスクリーンショット + API 証跡 + ギャラリー（index.html / FEATURES.md）。
      # どんな機能があるかはこのアーティファクトの index.html を開けば一発で分かる。
      - name: Upload feature catalog (screenshots)
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: feature-catalog
          path: e2e/artifacts/
          if-no-files-found: warn

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: playwright-report
          path: e2e/playwright-report/
          if-no-files-found: ignore
